version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
  pre_build:
    commands:
      - npm ci
  build:
    commands:
      - npm run bootstrap
      - npm run deploy:serverless
      - npm run deploy:backend-ecs