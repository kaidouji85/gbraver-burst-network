{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --ci",
    "start:serverless-stub": "lerna run --scope @gbraver-burst-network/serverless-stub --stream --no-prefix start:production",
    "clean": "lerna run clean",
    "build": "run-s build:core build:serverless build:browser-sdk build:serverless-stub",
    "build:core": "lerna run --scope @gbraver-burst-network/core build",
    "build:serverless": "lerna run --scope @gbraver-burst-network/serverless build",
    "build:browser-sdk": "lerna run --scope @gbraver-burst-network/browser-sdk build",
    "build:serverless-stub": "lerna run --scope @gbraver-burst-network/serverless-stub build",
    "serve:serverless-stub": "lerna run --scope @gbraver-burst-network/serverless-stub --stream --no-prefix serve",
    "deploy:serverless": "lerna run --scope @gbraver-burst-network/serverless deploy"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "npm-run-all": "^4.1.5"
  }
}
