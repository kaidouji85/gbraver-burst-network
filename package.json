{
  "name": "root",
  "private": true,
  "scripts": {
    "prepare": "npm-run-all bootstrap build",
    "bootstrap": "lerna bootstrap",
    "build": "npm-run-all build:core build:monosilic",
    "build:core": "lerna run --scope @gbraver-burst-network/core build",
    "build:monosilic": "lerna run --scope @gbraver-burst-network/core build"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "npm-run-all": "^4.1.5"
  }
}
