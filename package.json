{
  "name": "root",
  "private": true,
  "scripts": {
    "prepare": "npm-run-all bootstrap build",
    "bootstrap": "lerna bootstrap --ci",
    "build": "npm-run-all build:core build:monosilic-server",
    "build:core": "lerna run --scope @gbraver-burst-network/core build",
    "build:monosilic-server": "lerna run --scope @gbraver-burst-network/monosilic-server build"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "npm-run-all": "^4.1.5"
  }
}
