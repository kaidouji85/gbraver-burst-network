{
  "name": "root",
  "description": "gbraver burst network module.",
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "typescript": "^5.4.5"
  },
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna run clean",
    "code-format": "lerna run code-format",
    "code-format-check": "lerna run code-format-check",
    "detect-circular": "lerna run detect-circular",
    "lint": "lerna run lint",
    "lint-fix": "lerna run lint-fix",
    "test": "lerna run test"
  },
  "workspaces": [
    "packages/*"
  ]
}
