import { BattleEntry, BattleEntrySchema } from "../battle-entry";

/** 再戦エントリ */
export type RematchEntry = BattleEntry;

/** RematchEntry zodスキーマ */
export const RematchEntrySchema = BattleEntrySchema;
